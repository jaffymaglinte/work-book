import styled from '@emotion/styled';
import { Box } from '@mui/material';

const StyledCommentItemList = styled(Box)`
  & .comment_item {
    margin-bottom: 30px;
  }
`;

export default StyledCommentItemList;
