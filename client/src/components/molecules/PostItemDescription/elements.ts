import styled from '@emotion/styled';
import { Box } from '@mui/material';

const StyledPostItemDescription = styled(Box)`
  padding: 10px 20px;
  font-size: 15px;
`;

export default StyledPostItemDescription;
