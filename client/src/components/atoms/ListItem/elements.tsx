import { ListItem, ListItemProps as StyledListItemProps } from '@mui/material';
import styled from '@emotion/styled';

const StyledListItem = styled(ListItem)``;

export default StyledListItem;
export { type StyledListItemProps };
