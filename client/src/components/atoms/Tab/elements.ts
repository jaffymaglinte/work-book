import styled from '@emotion/styled';
import { Tab as MUI_Tab, TabProps as StyledTabProps } from '@mui/material';

const StyledTab = styled(MUI_Tab)``;

export default StyledTab;
export { type StyledTabProps };
