import { List, ListProps as StyledListProps } from '@mui/material';
import styled from '@emotion/styled';

const StyledList = styled(List)``;

export default StyledList;
export { type StyledListProps };
